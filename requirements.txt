# Essential requirements for Lecture Summarizer

streamlit
requests>=2.28.0
torch>=2.0.0
openai-whisper>=20231117
moviepy>=1.0.3
pydub>=0.25.1
psutil>=5.9.0
tqdm>=4.65.0
numpy>=1.21.0
pyaudioop
# The following are standard libraries and do not need to be installed via pip:
# os, tempfile, json, time, pathlib, uuid, datetime, logging, sys, subprocess, typing, dataclasses, warnings
